<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

    <!-- UID generator -->
    <bean id="disposableWorkerIdAssigner" class="com.baidu.fsg.uid.worker.DisposableWorkerIdAssigner"/>

    <bean id="defaultUidGenerator" class="com.baidu.fsg.uid.impl.DefaultUidGenerator" lazy-init="false">
        <property name="workerIdAssigner" ref="disposableWorkerIdAssigner"/>

        <!-- Specified bits & epoch as your demand. No specified the default value will be used -->
        <!--支持69年-->
        <property name="timeBits" value="41"/>
        <!--支持256个节点-->
        <property name="workerBits" value="8"/>
        <!--支持16535并发-->
        <property name="seqBits" value="14"/>
        <!--相对于时间基数的增量值-->
        <property name="epochStr" value="2050-01-01"/>
    </bean>

    <!-- Import mybatis config -->
    <!--<import resource="classpath:/uid/mybatis-spring.xml"/>-->

</beans>
